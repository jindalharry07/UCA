npx create-next-app@latest   works with react used for server side rendering
next js uses file based routing

node js run on windows/ mac/ interact directly with os . it is build on nextjs
NEXT JS compliteit(execute the js create the dom and there serve it to frontend)
frontend framework

what is server side rendering ?
Server-Side Rendering (SSR) is a technique used in web development where a web page is rendered on the server instead of the browser.

When a user visits a website:

Without SSR (Client-Side Rendering / CSR) — The browser first downloads a blank HTML page and JavaScript files. Then, JavaScript runs in the browser to build (render) the content dynamically.

With SSR — The server prepares the complete HTML page (with all the content already rendered) and sends it to the browser. The browser then just displays it.

// rendering techniques
CSR -> Client side rendering
SSR -> server side rendering
SSG -> static side generation
ISR -> Incremental static regeneration

most common use case is hybrid approach which is combination of all the above  hybrid - combination of all the above
 -> some of CSR
 -> some of SSR
 -> some of SSG
 -> some part of single page or application

// next js tries to render every component server side
"use client" there is a way to tell next js that use that component on client side rendering


Pm2 

layout.js file is entry point of next project

which component is made in page.js in run time auto goes in layout.js gives as children
esme vo components hote h jo hr page m show hotte h

React is single page application 
do do this ->
import Link from 'next/link';
<Link href="/login">Go to login</Link>
this is tag of next.js
does pre fetching

RSC react server side component


npm run build 
- build the app in production mode
npm run start

can't run localstorage features because next is server side it is client side (localstorage is browser feacture) // can use "use client" and useEffect shouble be used


"@/*": ["./src/*"]
this means that when i write any import statement and then it will automatic search in src folder

default export vs named export

suspense - jab tak site load nhi hote tb kya dikhna h!


-client side rendering
-server side rendering
 -- pre rendering - server renders the page (before)
 steps npm run build circle - (static )  prerendered as static content, f -> (dynamic)server rendered on demand
    It simulate api call behavior with a delay
  -- if i have writen settimeout it will take time to build 

export const revalidate = 0;
this command allows settimeout in rendering on every reload

agr koi or number likha 0 ke jagah toh eg (10 toh 10 reload ke baad rerender kre ga)
its called cache revalidation
-- if noboday refresh then it will do nothing for 1 year 

-- on demand server - side rendering  -> never a stale page visible to user


Cumitative layout shift
- it is when all the pages is not loaded and suddendly buttons go down after loading
this is due when img is loaded later 
to fix this we can hardcode the width and height of img